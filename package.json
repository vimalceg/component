{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "build:base": "concurrently 'npm -w packages/hooks run build -- -w' 'npm -w packages/base run build -- -w' ",
    "test": "echo \"Error: no test specified\""
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@testing-library/jest-dom": "^5.11.9",
    "@testing-library/react": "^11.2.5",
    "@testing-library/react-hooks": "^7.0.2",
    "@testing-library/user-event": "^13.0.10",
    "@zohodesk/react-cli": "0.0.1-beta.162",
    "concurrently": "^7.0.0",
    "lerna": "^4.0.0"
  }
}
